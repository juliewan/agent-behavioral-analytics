"""
Synthetic data generation: benign multi-agent traffic (traffic.py) plus one
injector per attack/failure pattern (scenarios.py). Seeded RNG throughout,
because the committed sample dataset must reproduce offline (PLANNING.md §2,
§5).
"""
